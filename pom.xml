<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.akka</groupId>
        <artifactId>akka-javasdk-parent</artifactId>
        <version>3.6.3</version>
    </parent>
    <groupId>io.akka</groupId>
    <artifactId>open-notebook-akka</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <name>open-notebook-akka</name>
    <dependencies>
        <!-- Local-file source ingestion's PDF text-layer extraction (SPEC-001 SS1/SS3), mirroring
             content-core's own non-Docling pdfplumber fallback for the text layer a PDF already
             carries. Scanned/image-only PDFs, tables, and formulas (the parts Docling's OCR and
             vision models exist for) are the named, narrower gap; see LocalFileExtraction. -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.3</version>
            <exclusions>
                <!-- Runtime already provides commons-logging 1.2; PDFBox pulls a newer one that
                     the Akka runtime's dependency-conflict enforcer rejects. -->
                <exclusion>
                    <groupId>commons-logging</groupId>
                    <artifactId>commons-logging</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <!-- OPEN_NOTEBOOK_ENCRYPTION_KEY is a required deployment env var (see README),
                 mirroring the source's own hard requirement for credential encryption at rest.
                 Tests set a fixed test-only value here rather than relying on the shell's
                 environment, so `mvn test`/`mvn verify` are reproducible without local setup. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <environmentVariables>
                        <OPEN_NOTEBOOK_ENCRYPTION_KEY>test-only-passphrase</OPEN_NOTEBOOK_ENCRYPTION_KEY>
                        <!-- UrlExtractionEngineTest starts its own fixed-port loopback HTTP servers here,
                             the same fixed-port-mock pattern as bench/probes/mock_provider.py:26510. -->
                        <FIRECRAWL_API_URL>http://127.0.0.1:26520</FIRECRAWL_API_URL>
                        <CRAWL4AI_API_URL>http://127.0.0.1:26521</CRAWL4AI_API_URL>
                    </environmentVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <environmentVariables>
                        <OPEN_NOTEBOOK_ENCRYPTION_KEY>test-only-passphrase</OPEN_NOTEBOOK_ENCRYPTION_KEY>
                        <!-- UrlExtractionEngineTest starts its own fixed-port loopback HTTP servers here,
                             the same fixed-port-mock pattern as bench/probes/mock_provider.py:26510. -->
                        <FIRECRAWL_API_URL>http://127.0.0.1:26520</FIRECRAWL_API_URL>
                        <CRAWL4AI_API_URL>http://127.0.0.1:26521</CRAWL4AI_API_URL>
                    </environmentVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
